<Window x:Class="Diary_App.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Diary_App"
        mc:Ignorable="d"
        Title="Дневник"
        SizeToContent="WidthAndHeight">
    <Window.Resources>
        <local:ButtonActionConverter x:Key="ButtonActionConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
    </Window.Resources>
    <StackPanel Margin="5">
        <Label Content="Создатель дневника" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Top"/>
        <Button Click="ButtonPath_Click" Content="{Binding Path}" Name="PathButton"/>
        <ItemsControl ItemsSource="{Binding Rows}" Name="UIRows" Grid.IsSharedSizeScope="True" Width="{Binding ElementName=PathButton, Path=Width}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition SharedSizeGroup="0"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Ellipse Grid.Column="0" Fill="RoyalBlue" Width="20" Height="20" 
                                 Visibility="{Binding IsCurrent, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        <Label Grid.Column="1" Content="{Binding Path=FolderName}"/>
                        <Button Grid.Column="2" Click="Button_Click" Content="{Binding Path=FolderCreated, Converter={StaticResource ButtonActionConverter}}"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</Window>
